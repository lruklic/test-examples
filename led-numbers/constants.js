// Refresh between two LED movements in ms
let REFRESH_TIME = 50;

// Countdown how long number stays in place x REFRESH_TIME
let COUNTDOWN_TIME = 20;

// Panel height, in LEDs
let LED_PANEL_HEIGHT = 9;

// Panel width, in LEDs
let LED_PANEL_WIDTH = 29;

// Number of values
let NUMBER_OF_VALUES = 4;

// Digit width in LEDs
let DIGIT_WIDTH = 5;

// Gap width in LEDs
let GAP_WIDTH = 3;